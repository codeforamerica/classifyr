<!DOCTYPE html>
<html>
  <head>
    <title>Datablade</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-800">
    <main class="flex flex-col h-screen justify-center align-middle">
      <div class="max-w-7xl my-auto mx-auto px-4 sm:px-6 md:px-8 bg-gray-100">

        <div class="flex items-center h-16 px-4">
          <img class="hidden lg:block h-12 w-auto" src="<%= image_path 'workflow-logo-indigo-500-mark-white-text.svg' %>" alt="Classifyr">
        </div>
        <p class="text-slate-800">Hello World!</p>
        <div class="w-full">
          <% if notice %>
            <p class="notice" id="notice"><%= notice %></p>
          <% end %>
          <% if alert %>
            <p class="alert alert-danger"><%= alert %></p>
          <% end %>
        </div>

        <%= yield %>
      </div>
    </main>
  </body>
</html>
